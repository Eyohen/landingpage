import { FaqSection } from '@/components/FaqSection'

/**
 * "See it in action" / FAQs section on the landing page — the accordion now
 * lives in the shared FaqSection component (also used by Talk to Sales and
 * Contact Us).
 */
export function SeeItInAction() {
  return <FaqSection />
}
