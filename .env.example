# Origin of the blog CMS. Used at build time by scripts/fetch-content.ts and at
# runtime by the /blog/preview route. Leave unset locally to build from the
# committed manifest. Required in CI — a deploy without it would ship stale
# content, so the build fails instead.
VITE_CMS_URL=https://blog-cms-production-cd5f.up.railway.app
